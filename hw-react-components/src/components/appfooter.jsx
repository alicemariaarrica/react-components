export default function appFooter() {

    // logic
    const footer = "lorem ipsum etc etc"

    // markup (JSX)
    return (
        <footer>
            <div className="container">
                <p>{footer}</p>
            </div>
        </footer>
    )
}